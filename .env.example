# FoodLens configuration — copy to .env and adjust.
# LLM provider: template (default, zero deps) | local (flan-t5-base) | anthropic | openai
LLM_PROVIDER=template
# Only needed when LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=
# Only needed when LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Device override: auto | cpu | cuda
DEVICE=auto
# DataLoader workers (Windows: keep 0)
NUM_WORKERS=0
# Database URL
DATABASE_URL=sqlite:///foodlens.db
